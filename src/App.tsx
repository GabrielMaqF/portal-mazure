import { LoginPage } from './pages/LoginPage';
import './index.css'; // Importa os estilos globais (body, etc)

function App() {
  // Por enquanto, o App apenas renderiza a página de login.
  // No futuro, ele terá o sistema de rotas.
  return <LoginPage />;
}

export default App;